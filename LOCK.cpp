#include <bits/stdc++.h>
using namespace std;

int main()
{
	freopen("LOCK.INP","r",stdin);
	freopen("LOCK.OUT","w",stdout);
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int n,k,a[10000],b[10000];
	cin >> n >> k;
	for(int i=0;i<n;i++)
	cin >> a[i];
	for(int i=0;i<n;i++)
	cin >> b[i];
	cout << n;
}
